{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && pnpm install --no-frozen-lockfile && pnpm build && npm i -g @google/gemini-cli && ln -sf /app/server/node_modules/@google/gemini-cli/bundle/gemini.js /usr/local/bin/gemini && chmod +x /usr/local/bin/gemini && which gemini && gemini --version || true"
  },
  "deploy": {
    "startCommand": "PATH=/usr/local/bin:/app/server/node_modules/.bin:/app/node_modules/.bin:$PATH node --import ./server/node_modules/tsx/dist/loader.mjs server/dist/index.js",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
